import type { NextConfig } from "next";

// Hardens responses against clickjacking, MIME-sniffing, and referrer leakage.
// CSP is intentionally omitted here — Stripe/Supabase SDKs each bring their
// own sources and a strict CSP without tooling would block them. Add one via
// middleware with nonces when ready.
const securityHeaders = [
  { key: "X-Frame-Options", value: "DENY" },
  { key: "X-Content-Type-Options", value: "nosniff" },
  { key: "Referrer-Policy", value: "strict-origin-when-cross-origin" },
  {
    key: "Permissions-Policy",
    value: "camera=(), microphone=(), geolocation=(), interest-cohort=()",
  },
  {
    key: "Strict-Transport-Security",
    value: "max-age=63072000; includeSubDomains; preload",
  },
];

const nextConfig: NextConfig = {
  async headers() {
    return [
      {
        source: "/:path*",
        headers: securityHeaders,
      },
    ];
  },
};

export default nextConfig;
